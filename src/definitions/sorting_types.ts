export enum Sorting {
  Unset = "unset", // state before an method is set, shall never appear in configuration files
  Disabled = "disabled",
  AsString = "as_string",
  AsNumber = "as_number",
  AsDate = "as_date",
}
